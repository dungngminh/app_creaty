import 'package:flutter/material.dart';

class ListComponentPanel extends StatelessWidget {
  const ListComponentPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
